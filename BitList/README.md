### ㅁ BitList

```
#
TRUE==1 if val > 0
FALSE==0 if val <= 0

# user function #
create_BitList(len, TRUE or FALSE)
destroy_BitList(list)
set_BitList(list, idx, TRUE or FALSE)
get_BitList(list, idx)
not_BitList(list)
print_BitList(list, jump)

```
